# Optional search API providers for scripts/collect-news.mjs.
# The daily Codex automation can still use browser/search tools without these,
# but API keys make the raw candidate pool better.

TAVILY_API_KEY=
EXA_API_KEY=
GITHUB_TOKEN=

